# FleetTrack
Pipeline de dados (Bronze/Silver/Gold) com a API SPTrans Olho Vivo, dados de ônibus de SP em tempo real. Ingestão, limpeza incremental, agregações e orquestração com Prefect.

Data pipeline (Bronze/Silver/Gold) using São Paulo's SPTrans Olho Vivo API for real-time bus data. Ingestion, incremental cleaning, aggregations, and Prefect orchestration.
